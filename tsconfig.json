{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [
    "packages/*/src/**/*.ts",
    "apps/api/src/**/*.ts"
  ],
  "comment": "apps/web is typechecked by its own tsconfig (has vite/client types + JSX). Root typecheck covers backend + shared/data. / apps/web 由其自身 tsconfig 检查（含 vite 类型+JSX）；根 typecheck 覆盖后端与 shared/data。",
  "exclude": ["node_modules", "**/dist", "**/node_modules", "**/*.test.ts", "**/test/**"]
}
